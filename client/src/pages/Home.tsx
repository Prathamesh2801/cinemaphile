import { Layout } from "../layout/Layout";
import { SearchBar } from "../components/SearchBar";

export const Home = () => {
  return (
    <>
      <Layout>
        <SearchBar />
      </Layout>
    </>
  );
};
